export default function NotFound() {
    return (
        <main className="not-found">
            <h1>Page  not found</h1>
            <p>Cannot find the requested resource or page.</p>
        </main>
    );
}